doctype html
html(lang="en" style=`background-image:url(bg/${content.backgroundImage});background-size:${content.backgroundSize}`)
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="stil.css")
        title ATAMA SORGU
    body(style=content.karartma=='on' && "background-image:radial-gradient(rgba(0, 0, 0, 0.3) 60%,  rgb(0, 0, 0))")
        div(style=`box-shadow:${content.shadow=='on' && 'rgba(0,0,0,0.7) 0 0 20px'}`, class=`container ${content.formtema}`)
            block clientcontent

